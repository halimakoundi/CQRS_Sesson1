using MovingToCQRS.Src;
using NUnit.Framework;

namespace MovingToCQRS.Tests
{
    [TestFixture]
    public class UserRepoQueryShould
    {
        [Test]
        public void find_user_by_userName()
        {
        //    var userName = "User UserName";
        //    var userRepoQuery = new UserRepoQuery();
        //    userRepoQuery.CreateUser(userName, _name);

        //    var retrievedUser = userRepoQuery.FindByUserName(userName);

        //    Assert.That(retrievedUser.UserName, Is.EqualTo(userName));
        }
    }
}
